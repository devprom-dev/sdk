imports:
    - { resource: config.yml }

parameters:
    db_host: localhost
    db_user: devprom
    db_pass: devprompass
    db_name: devprom
    app_charset: utf-8

    systemLanguage: ru
    adminEmail: admin@domain
    supportEmail: support@domain
    supportName: Test Support

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file